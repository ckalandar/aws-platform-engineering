apiVersion: karpenter.k8s.aws/v1

kind: EC2NodeClass

metadata:
  name: default

spec:

  role: kk-platform-dev-karpenter-node

  amiSelectorTerms:
    - alias: al2023@latest

  subnetSelectorTerms:
    - tags:
        karpenter.sh/discovery: kk-platform-dev

  securityGroupSelectorTerms:
    - tags:
        karpenter.sh/discovery: kk-platform-dev

  tags:
    karpenter.sh/discovery: kk-platform-dev
