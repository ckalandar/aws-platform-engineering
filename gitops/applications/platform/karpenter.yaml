apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: karpenter
  namespace: argocd

spec:
  project: default

  destination:
    namespace: karpenter
    server: https://kubernetes.default.svc

  sources:

    - repoURL: oci://public.ecr.aws/karpenter/karpenter
      chart: karpenter
      targetRevision: 1.6.1

      helm:
        values: |
          serviceAccount:
            create: false
            name: karpenter

          settings:
            clusterName: kk-platform-dev
            clusterEndpoint: https://localhost:6501

          replicas: 0

  syncPolicy:
    automated:
      prune: true
      selfHeal: true

    syncOptions:
      - CreateNamespace=true
